export class DeLession {
    vocabulary: Map<string, string>;
    sPattern: Array<string>;
    phonics: Map<string, Array<string>>;

    // constructor(v:Array<string>, sP: Array<string>, p:Map<string, Array<string>>){
    //     this.vocabulary = v;
    //     this.sPattern = sP;
    //     this.phonics = p;
    // }
}